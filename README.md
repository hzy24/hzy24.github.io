
# Academic Portfolio & Homepage

This is my personal academic website, tailored for researchers and graduate students. 

Originally forked from the creative and fun portfolio by [rulanugrh](https://github.com/rulanugrh/rulanugrh.github.io).

<!-- ## 🚀 Key Optimizations & Upgrades

### 1. ⚙️ Under-the-Hood Engineering (Modular Architecture)
The biggest upgrade is the transition from a monolithic, hard-coded HTML file to a dynamic, component-based architecture:
* **Component Splitting:** Broken down the massive `index.html` into easily maintainable standalone modules (`home.html`, `education.html`, `research.html`, etc.) located in the `sections/` directory.
* **Modern Asynchronous Loading:** Stripped out legacy jQuery dependencies. Built a custom, modern `fetch` API engine with `Promise.all` to asynchronously load DOM fragments.
* **Dynamic Navigation Generation:** Implemented a JavaScript `pageConfig` as the "Source of Truth". The top navigation bar is now dynamically generated based on active modules, allowing for instant tab renaming or toggling (e.g., turning on/off the "About" section) simply by changing a boolean in the JS config.
* **Animation Synchronization:** Re-engineered the initialization of `ScrollReveal.js` to ensure smooth slide-in animations trigger *only after* all asynchronous DOM modules are fully loaded and rendered.

### 2. 🎨 Visual & UI Overhaul
Redesigned the visual hierarchy to reflect the rigor and calmness expected in academia (specifically Astronomy and AI):
* **Deep-Space / Neural Network Particles:** Tweaked the `particles.js` configuration on the cover page. Removed the distracting, fast-moving, multi-colored shapes in favor of a slow-drifting, subtle white/blue node network that perfectly metaphorizes Astrophysics and Machine Learning.
* **Distraction-Free Reading:** Completely stripped the particle background from the detailed content pages to ensure maximum readability for visiting professors and admission committees.
* **Classic Academic Layout:** Rebuilt the Hero section using CSS Flexbox to achieve a perfectly balanced "Left Text, Right Photo" layout. 
* **Refined Typography & Shapes:** Replaced oversized text and jarring orange buttons with a sleek "VS Code Blue" (`#007acc`) color palette, responsive auto-width buttons, and a perfectly circular cropped profile picture (`aspect-ratio: 1/1`).

### 3. 🎓 Academic-Focused Content
Shifted the content focus from generic web development skills to research output:
* Integrated **Academicons** & FontAwesome to elegantly link to external research profiles (Google Scholar, INSPIRE-HEP, ORCID) rather than manually maintaining a bulky list of publications.
* Restructured sections to prioritize `Education`, `Research Tastes`, and `CV Download`.

## 🛠️ How to Maintain (For Personal Reference)
To add, remove, or rename a section on the website:
1. Open `portfolio/index.html`.
2. Scroll down to the `<script>` tag and locate the `pageConfig` object.
3. Toggle `loadByDefault: true/false` to show or hide a section, or edit `navTitle` to change its display name on the navigation bar. 
4. Edit the specific content inside `portfolio/sections/{filename}.html`. -->
